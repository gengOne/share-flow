// Backend configuration
// Change this to switch between mock and real backend

// For development with mock data (no Rust service needed)
// export { backend } from './mockBackend';

// For production with real Rust service
export { backend } from './realBackend';
